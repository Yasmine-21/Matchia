import { describe, it, expect } from 'vitest';
import * as moduleVisibility from './moduleVisibility';

describe('moduleVisibility utils', () => {
  it('should be defined', () => {
    expect(moduleVisibility).toBeDefined();
  });
});
