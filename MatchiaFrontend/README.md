
  # Design Matchia SaaS Application

  This repository contains the frontend code for the Matchia SaaS application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  