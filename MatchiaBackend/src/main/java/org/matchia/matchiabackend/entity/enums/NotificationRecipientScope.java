package org.matchia.matchiabackend.entity.enums;

/** Distinguishes bank-shared notifications from notifications addressed to one user. */
public enum NotificationRecipientScope {
    BANK,
    USER
}
